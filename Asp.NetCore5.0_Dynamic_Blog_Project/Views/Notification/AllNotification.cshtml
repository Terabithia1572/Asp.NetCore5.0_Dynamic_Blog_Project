@using EntityLayer.Concrete
@model List<Notification>
@{
    ViewData["Title"] = "AllNotification";
    Layout = "~/Views/Shared/WriterLayout.cshtml";
}


<br />

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <table class="table table-bordered">

                <thead>
                    <tr>
                        <th> # </th>
                        <th> Bildirimler </th>
                        <th> Ne Zaman </th>
                        <th> Tarih  </th>

                    </tr>

                </thead>

                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td> @item.NotificationID </td>
                            <td> @item.NotificationDetails </td>
                            <td>
                                <div class="progress">
                                    <div class="progress-bar bg-success" role="progressbar" style="width: 50%" aria-valuenow="90" aria-valuemin="25" aria-valuemax="100"></div>
                                </div>
                            </td>
                            <td>
                                @{
                                    @*var sayi = Convert.ToDateTime((DateTime.Now- item.NotificationDate).ToString()).ToShortDateString();
                                        @sayi.ToString().Substring(0,2)*@
                                    @*var sayi1 = DateTime.Now.Day.ToString();
                                        var sayi2 = item.NotificationDate.Day.ToString();
                                        var sonuc = DateTime.Parse(sayi1) - DateTime.Parse(sayi2);
                                        @sonuc.ToString()*@
                                    @*int sonuc = string.Compare(sayi1, sayi2);
                                        @sonuc.ToString()*@

                                    var minute = DateTime.Now.Subtract(item.NotificationDate).TotalMinutes;
                                    string sonuc = " Dakika Önce Gönderildi..";
                                    @minute.ToString().Substring(0, 4) @sonuc

                                }
                            </td>
                            @*<td> @item.NotificationDate.ToShortDateString() </td>*@
                        </tr>
                    }
                </tbody>

            </table>
        </div>
    </div>
</div>

